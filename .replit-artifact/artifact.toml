kind = "web"
previewPath = "/"
title = "Aplikasi Penjemputan Siswa"
version = "1.0.0"
id = "artifacts/penjemputan-siswa"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20221

[services.development]
run = "pnpm --filter @workspace/penjemputan-siswa run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/penjemputan-siswa", "run", "build" ]
publicDir = "artifacts/penjemputan-siswa/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20221"
BASE_PATH = "/"
